// Exercise 4 - 14. Define a macro swap(t, x, y) that interchanges two arguments of type t.(Block structure will help.)

#include <stdio.h>
#include <string.h>
#include <ctype.h>

#define swap(t) printf("t is " #t)

int main()
{
  swap(1);
}

// I really don't get what they are asking, interchange two arguments of type t?